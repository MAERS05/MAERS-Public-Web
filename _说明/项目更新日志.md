# MAERS - 项目更新日志

## [2026-03-05]
### 🚀 功能 (Feat)
- **Games 模块**: 为游戏前台展现新增 `Playing / Played` 双视图切换模式。
  - 在 `#games-container` 标题栏上添加视图切换按钮。
  - 添加 `#playing-container` 占位，为之后扩展 “正在游玩” 视图。

### 🔄 重构 (Refactor)
- **Games Admin 模块并入 CMS Admin 架构**: 
  - 彻底删除了独立的 `admin-games.html` 入口文件，减少 110 行亢余代码。
  - 修改 `data/admin-portal.json` 配置，使游戏后台管理从统配入口 `admin-cms.html?module=games` 进入。
  - 在 `custom/cms/admin-main.module.js` 适配层加入 `GamesView` 的自动分发逻辑，以确保与 Literature 模块高度一致，增强通用 CMS 底座的代码复用率和可维护性。
  - 添加 `!State.IS_ADMIN` 的保护，确保视图切换层逻辑仅约束前台，不再泄露并干扰 Admin 组件运行。
